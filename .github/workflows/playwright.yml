name: Allure Report Integration

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '16'

    - name: Install dependencies
      run: npm install

    - name: Run tests and generate Allure results
      run: |
        npm test
        npx allure generate allure-results --clean -o allure-report

    - name: Upload Allure results
      uses: actions/upload-artifact@v3
      with:
        name: allure-results
        path: allure-results

    - name: Deploy Allure Report to GitHub Pages
      if: success()
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GHA_PASSCODE }}
        publish_dir: ./allure-report

    - name: Display Allure Report URL
      run: |
        echo "Allure Report URL: https://manjunathnp.github.io/Playwright-AutomationFramework-POM/allure-report"



# name: Playwright SauceDemo Automation Tests

# on:
#   push:
#     branches: [ main, master ]
#   pull_request:
#     branches: [ main, master ]
#   # schedule:
#   #   - cron: '0 * * * *' # Runs at the beginning of every hour
#   #

# jobs:
#   test:
#     timeout-minutes: 60
#     runs-on: ubuntu-latest

#     steps:
#     - uses: actions/checkout@v4
#     - uses: actions/setup-node@v4
#       with:
#         node-version: lts/*
    
#     - name: Install dependencies
#       run: npm ci

#     - name: Install Playwright Browsers
#       run: npx playwright install --with-deps

#     - name: Run Playwright tests
#       run: npm run test
    
#     - name: Get Allure history
#       uses: actions/checkout@v3
#       if: always()
#       continue-on-error: true
#       with:
#         ref: gh-pages
#         path: gh-pages

#     - name: Allure Report action from marketplace
#       uses: simple-elf/allure-report-action@master
#       if: always()
#       id: allure-report
#       with:
#         allure_results: allure-results
#         gh_pages: gh-pages
#         allure_report: allure-report
#         allure_history: allure-history

#     - name: Deploy report to Github Pages
#       if: always()
#       uses: peaceiris/actions-gh-pages@v3
#       env:
#         PERSONAL_TOKEN: ${{ secrets.GHA_PASSCODE }}
#         PUBLISH_BRANCH: gh-pages
#         PUBLISH_DIR: allure-history



# name: Playwright SauceDemo Automation Tests

# on:
#   push:
#     branches: [ main, master ]
#   pull_request:
#     branches: [ main, master ]
#   # schedule:
#   #   - cron: '0 * * * *' # Runs at the beginning of every hour
#   #

# jobs:
#   test:
#     timeout-minutes: 60
#     runs-on: ubuntu-latest

#     steps:
#     - uses: actions/checkout@v4
#     - uses: actions/setup-node@v4
#       with:
#         node-version: lts/*
    
#     - name: Install dependencies
#       run: npm ci

#     - name: Install Playwright Browsers
#       run: npx playwright install --with-deps

#     - name: Run Playwright tests
#       run: npm run test

#     - name: Generate Allure Report
#       run: npx allure generate allure-results --clean -o allure-report

#     - name: Upload Allure Report
#       uses: actions/upload-artifact@v4
#       if: always()
#       with:
#         name: allure-report
#         path: allure-report/
#         retention-days: 30

#     - name: Upload Playwright Report
#       uses: actions/upload-artifact@v4
#       if: always()
#       with:
#         name: playwright-report
#         path: test-reports/
#         retention-days: 30

#     - name: Deploy Allure Report to GitHub Pages
#       if: always()
#       uses: peaceiris/actions-gh-pages@v3
#       with:
#         github_token: ${{ secrets.GHA_PASSCODE }}
#         publish_dir: ./allure-report


# name: Playwright SauceDemo Automation Tests

# on:
#   push:
#     branches: [ main, master ]
#   pull_request:
#     branches: [ main, master ]
#   # schedule:
#   #   - cron: '0 * * * *' # Runs at the beginning of every hour

# jobs:
#   test:
#     timeout-minutes: 60
#     runs-on: ubuntu-latest

#     steps:
#     - uses: actions/checkout@v4
#     - uses: actions/setup-node@v4
#       with:
#         node-version: lts/*
    
#     - name: Install dependencies
#       run: npm ci

#     - name: Install Playwright Browsers
#       run: npx playwright install --with-deps

#     - name: Run Playwright tests
#       run: npm run test

#     - name: Generate Allure Report
#       run: npx allure generate allure-results --clean

#     - name: Upload Allure Report
#       uses: actions/upload-artifact@v4
#       if: always()
#       with:
#         name: allure-report
#         path: allure-report/
#         retention-days: 30

#     - name: Upload Playwright Report
#       uses: actions/upload-artifact@v4
#       if: always()
#       with:
#         name: playwright-report
#         path: test-reports/
#         retention-days: 30
    
#     - name: Load test report history
#       uses: actions/checkout@v4
#       if: always()
#       continue-on-error: true
#       with:
#         ref: gh-pages
#         path: gh-pages-dir

#     - name: Build test report
#       uses: simple-elf/allure-report-action@v1.7
#       if: always()
#       with:
#         gh_pages: gh-pages-dir
#         allure_history: allure-history
#         allure_results: build/allure-results

#     - name: Publish test report
#       uses: peaceiris/actions-gh-pages@v3
#       if: always()
#       with:
#         github_token: ${{ secrets.GITHUB_TOKEN }}
#         publish_branch: gh-pages
#         publish_dir: allure-history